// The default Flutter counter smoke test was removed — it tested a widget
// that does not exist in this app and failed immediately.
//
// Tests to add as the codebase matures:
//   - Dot.fromJson / toJson round-trip (no Flutter or Supabase needed)
//   - Project.fromMap / toMap round-trip
//   - Arc-length resampling logic (if ported to Dart)
//   - PdfService page layout (using pdf package's test utilities)
//   - Auth flow widget test with a mocked Supabase client

void main() {}
